# 零基础入门教程

这份教程假设你完全没有编程基础。
但是没关系，你只需要按步骤照做即可。

## 0 基础超简单教程

下面假设你是完全没编程基础的人。

## 第 1 步：先准备工具

电脑上需要安装两个东西：

1. Node.js  
   用来运行插件命令。

2. Git  
   用来拉取和提交 Gitee 项目。

装好后打开 PowerShell，输入：

```bash
node -v
git --version
```

如果能看到版本号，就说明安装好了。

如果团队已经把 Project Guardian 安装成全局命令，继续确认一下：

```bash
guardian --version
```

如果提示找不到 `guardian`，也没关系，后面可以继续使用 `node plugins/project-guardian/scripts/guardian.js` 这种项目内路径。

## 第 2 步：拿到项目

如果项目已经在 Gitee，先复制仓库地址，然后运行：

```bash
git clone 你的Gitee仓库地址
```

例如：

```bash
git clone https://gitee.com/company/demo-project.git
```

然后进入项目目录：

```bash
cd demo-project
```

这个目录就叫“项目根目录”。

## 第 3 步：确认 Project Guardian 怎么运行

Project Guardian 有两种使用方式。

第一种是全局命令。如果这个命令能运行：

```bash
guardian --version
```

说明你可以在任何项目里直接用 `guardian`。

第二种是项目内置插件。项目里应该有这个文件：

```text
plugins/project-guardian/scripts/guardian.js
```

如果有，说明插件源码已经放在项目里。没有全局命令时，就使用：

```bash
node plugins/project-guardian/scripts/guardian.js
```

## 第 4 步：初始化项目记忆

在项目根目录运行：

```bash
guardian init
```

它会生成这些文件：

```text
memory/PROJECT_CONTEXT.md
memory/STATE.md
memory/DECISIONS.md
memory/AI_CHANGELOG.md
memory/HANDOVER.md
AGENTS.md
.cursorrules
.cursor/rules/project-guardian.mdc
```

如果你的电脑还没有全局 `guardian` 命令，就改用：

```bash
node plugins/project-guardian/scripts/guardian.js init
```

这些就是“项目记忆”。

如果你的团队使用 Cursor、VS Code、Copilot、Windsurf、Cline、Continue、Claude Code 或 Gemini CLI，可以按实际使用的工具安装适配规则：

```bash
guardian install-adapters --adapter cursor,copilot,windsurf,cline,continue,claude,gemini,vscode
guardian adapters doctor
```

如果没有全局命令，就把上面的 `guardian` 换成：

```bash
node plugins/project-guardian/scripts/guardian.js
```

如果你的 AI IDE 支持 MCP，可以让它直接调用 Project Guardian 工具。全局命令模式填写：

```text
command: guardian
args: mcp
```

项目内置插件模式填写：

```text
command: node
args: plugins/project-guardian/scripts/guardian.js mcp
```

如果你担心 AI IDE 误调用写入命令，先把 `project-guardian.config.json` 里的 MCP 改成只读：

```json
{
  "mcp": {
    "readOnly": true,
    "allowedTools": ["guardian_brief", "guardian_query", "guardian_verify", "guardian_doctor"]
  }
}
```

只读后，AI IDE 只能查询和检查，不能通过 MCP 自动更新记忆或生成交接。

## 第 5 步：检查是否接入成功

运行：

```bash
guardian doctor
```

如果看到：

```text
Core memory files: ok
AI rule files: ok
```

说明基础接入成功。

## 第 6 步：让 AI 帮你补第一版文档

把这段话复制给 AI：

```text
请先运行 guardian brief "补第一版项目记忆"，再阅读当前项目代码，不要修改业务代码。请按照 Project Guardian 标准补齐 memory/PROJECT_CONTEXT.md、memory/STATE.md 和 memory/DECISIONS.md，重点写清楚项目目标、技术栈、运行方式、核心业务流程、当前状态、已知问题和风险区域。
```

注意：AI 写完后，人要看一遍，防止它乱猜。

## 第 7 步：检查文档有没有太空

运行：

```bash
guardian validate-docs
```

刚初始化时失败是正常的，因为文档还是空模板。  
补完内容后再运行，应该通过。

## 第 8 步：每天开发前怎么做

每天开始写代码前，先运行：

```bash
git pull
guardian doctor
guardian brief "今天继续开发"
```

然后先读这两个文件：

```text
memory/PROJECT_CONTEXT.md
memory/STATE.md
```

如果 `brief` 里推荐了 `memory/DECISIONS.md`、`memory/AI_CHANGELOG.md` 或 `memory/HANDOVER.md`，再继续读。这样不会每次都把所有历史都塞给 AI，token 消耗会低很多。

如果今天是修 bug、测试失败、改登录/权限/支付/数据、看不懂历史原因，直接用深度读取：

```bash
guardian brief "今天继续开发" --mode deep
```

如果是新人接手、交接、上线前检查、大重构，直接用完整读取：

```bash
guardian brief "新人接手" --mode full
```

记住：少读只是为了省钱省上下文，不是禁止 AI 多读。风险变高时必须让 AI 多读。

你可以问 AI：

```text
请先运行 guardian brief "今天继续开发"，再按读取计划告诉我这个项目现在做到哪了，今天适合继续做什么，有哪些风险。
```

## 第 9 步：AI 改完代码后怎么做

比如你让 AI 做了“登录验证码”。

改完后运行：

```bash
guardian update "实现登录验证码"
```

它会更新：

```text
memory/STATE.md
memory/AI_CHANGELOG.md
```

然后你要打开 `memory/AI_CHANGELOG.md`，把里面的 TODO 补清楚。

注意：新记录标题里的时间要用真实当前时间，例如 `2026-06-02 17:50`，不要写成 `00:00` 占位。

## 第 10 步：提交前检查

提交前运行：

```bash
git add .
guardian verify
```

如果都通过，再提交：

```bash
git commit -m "feat: add login captcha"
git push
```

## 第 11 步：换人交接怎么做

如果实习生离职、换人、暂停项目、上线前，运行：

```bash
guardian update "阶段交接前整理"
guardian handover
```

它会生成或刷新：

```text
memory/HANDOVER.md
```

新人来了先读这个文件。

## 第 12 步：新人接手怎么做

新人第一天不要急着改代码。

先运行：

```bash
git pull
guardian doctor
guardian brief "新人接手"
```

然后先读：

```text
memory/PROJECT_CONTEXT.md
memory/STATE.md
```

如果 `brief` 推荐交接文档，再读 `memory/HANDOVER.md`；如果推荐历史和决策，再读 `memory/DECISIONS.md` 和 `memory/AI_CHANGELOG.md`。

再运行：

```bash
guardian query
```

如果只是想问一个问题，并且不希望 AI 一次读太多片段，可以这样限制返回数量：

```bash
guardian query "登录流程" --limit 3
```

`--limit` 可以填 1 到 10。日常答疑建议先用 2 到 3，结果不够再加大。

可以问：

```text
这个项目现在做到哪了？
我今天应该先做什么？
登录模块入口在哪里？
最近一次 AI 修改改了什么？
哪些文件最容易出问题？
```

退出输入：

```text
exit
```

## 第 13 步：安装本地自动检查

如果想每次提交前自动检查，运行：

```bash
guardian install-hooks
```

以后 `git commit` 时会自动检查。

默认 hook 会运行三类检查：代码变更是否带上项目记忆、记忆文档是否仍是空模板、记忆文件里是否写入了疑似密钥。

## 第 14 步：安装 Gitee 自动检查

如果要让 Gitee 推送后自动检查，运行：

```bash
guardian install-ci
```

它会生成：

```text
.workflow/project-guardian.yml
```

注意：默认分支是 `master`。  
如果你的 Gitee 主分支叫 `main` 或 `develop`，要打开 `.workflow/project-guardian.yml`，把：

```yaml
branch: master
```

改成：

```yaml
branch: main
```

或：

```yaml
branch: develop
```

## 熟练使用口诀

每天开发：

```bash
git pull
guardian doctor
guardian brief "今天继续开发"
读 memory/PROJECT_CONTEXT.md 和 memory/STATE.md
让 AI 改代码
guardian update "任务说明"
guardian verify
git commit
git push
```

换人交接：

```bash
guardian update "阶段交接前整理"
guardian handover
guardian verify
git commit
git push
```

新人接手：

```bash
guardian brief "新人接手"
按 brief 结果读必要记忆
guardian query "新人接手" --limit 3
先做小任务
改完 update
提交前 guardian verify
```

## 现在最重要的一个命令

如果你只记住一个检查命令，就记住：

```bash
guardian verify
```

它会帮你一次性检查：项目是否接入完整、代码变更有没有更新记忆、文档是不是还停留在空模板、记忆里有没有疑似密码或 token。

## 当前够不够用

够内部使用了。现在已经有：

```text
初始化
项目记忆
AI 修改记录
交接文档
本地查询
提交检查
文档质量检查
Git Hook
Gitee CI
Codex skill
Cursor rules
Copilot instructions
Windsurf rules
Cline rules
Continue rules
Claude Code CLAUDE.md
Gemini CLI GEMINI.md
VS Code tasks
MCP 工具调用
适配器体检
安全扫描
决策复审
自动化测试
配置文件 project-guardian.config.json
```

下一步如果要更通用，可以做：

```text
真正 RAG / 向量检索
VS Code 原生扩展
JetBrains 插件
GitHub Actions / GitLab CI 模板
```

## 提交前最重要提醒

如果你要提交到 Gitee，并且还想保留 Codex 插件能力，请不要让这个文件从 Git 里删除：

```text
plugins/project-guardian/.codex-plugin/plugin.json
```

这个文件是 Codex 插件元数据。如果团队不用 Codex，也可以只使用 CLI 和其它 AI IDE 适配器；但发布 Project Guardian 插件本身时应保留它。
